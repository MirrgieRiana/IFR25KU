# AI のための共通指示文

このファイルは、ファイル名に反して、Codex だけではなく、すべての AI エージェントが守るべき規則が記述されています。
人間は、必ずしもこの指示を守る必要はありません。

## ソースコードの入手

### `genSources` タスク - Minecraft のソースコードの jar を生成する

Minecraft のソースコードを逆コンパイルおよび難読化解除した jar を生成します。
出力先は `{projectDir}/.gradle/loom-cache/minecraftMaven/net/minecraft/` 配下です。

### `unpackSources` タスク - Minecraft を含むすべての外部依存関係のソースコードを展開する

`unpackSources` タスクは、Minecraft および sources jar を提供しているすべての外部依存関係の難読化解除されたソースコードを収集し、展開します。
出力先は各サブプロジェクトの `unpackedSources` ディレクトリ内です。

あなたがこのリポジトリ上で AI エージェントとして活動する際は、`unpackedSources` が展開済みであることを一旦期待してください。
もしリポジトリに空でない `unpackedSources` ディレクトリが存在する場合、 **Minecraftの難読化解除されたソースコードも必ずその中にあります** 。

このとき、キーワード検索が期待に反してヒットしない場合、その原因は 100% の確率であなたの探し方や検索語句が誤っていることにあります。
Minecraft のコードのうち一部が展開されていないという可能性も、決してありません。
あなたの探し方をあなたが工夫してください。資料は既にあります。

もし展開済みでない場合、あなたは `unpackSources` を実行することで、時間はかかるもののソースコードを手に入れることができます。
可能な限りソースコードを参照しながら実装を行うことを強く推奨します。

### `scripts/unpack-assets.sh` スクリプト - Minecraft の配布アセットを取得する

このスクリプトは、作業に必要なアセットを Minecraft の配布サーバーから取得し、`unpackedAssets` 以下にダウンロードします。

ダウンロード対象には、少なくとも以下のファイルが含まれます。

- 日本語翻訳ファイル

あなたがこのリポジトリ上で AI エージェントとして活動する際は、`unpackedAssets` が生成済みであることを一旦期待してください。
もし生成済みでなかった場合は、必要に応じて、当該スクリプトを実行することにより、アセットファイルを取得してください。

## Lang Table

以下のファイルは、本 MOD のテキストリソースをすべて収録した 1 個のファイルです。

- https://ifr25ku.mirrgieriana.net/lang_table.jsonl

正式名称の調査のほか、世界観の理解において最も重要なファイルです。

## 開発

コンテンツは、最初から独自に構築するのではなく、参考にできる既存の類似コンテンツを探してください。
それは MOD のコンテンツだけでなく、バニラのものも探す必要があります。
そして、参考にすべき既存のコンテンツの識別子で、バニラ、Fabric API、および MOD のコードに全文検索をかけてください。
そして、当該識別子の出現箇所ごとに、こちらでも同様のものを実装すべきか否か、考察してください。
ユーザーへの報告では、参考にしたすべての既存コンテンツごとに、すべての出現箇所について、考察の結果を表形式でまとめてください。

### マトリクス状の参考構造

参考にすべきコンテンツは必ずしも 1 個とは限らず、複数のコンテンツや、「コンテンツ同士の差分」を参考にすべき状況もあります。
例えば、「氷の階段」を作るのに、「レンガの階段」を参考にしたとします。
そこに、「レンガと氷の違い」を取り込むことで、「氷の階段」がとりあえずできます。

そして、更に、場合によっては、単純な掛け算ではうまく行かない場合もあり、そこも考慮しなければなりません。
例えば、氷の階段が融けたら「水源」になるべきなのか、「水源の階段」になるべきか、という哲学的な問題が生じます。

### パラメータ

アイテムやその他のゲームコンテンツの数値、もしくは列挙パラメータを設定する場合は、よく吟味してください。
バニラ、MOD 固有問わず、既存の類似のコンテンツとの比較を行い、妥当と考えられるパラメータを検討してください。
既存コンテンツのコピーで設定するのではなく、設定しようとしている対象の意味論的位置づけも加味してください。
報告では、比較表を付記しながら、そのパラメータ設定の妥当性について説明してください。

### 名称

とりあえず、最も妥当と考えられる内容を適当に考えて記入してください。

### アイテムポエム

MOD の世界観設定に由来しないコンテンツには、基本的にポエムはありません。
たとえば蛍石は現実世界のものと同じであるため、ポエムを持ちません。
一方、ルミナイトは MOD の世界観に由来するアイテムであるため、ポエムを持ちます。

ポエムの文面を埋める必要がある場合、あなたは、適当にそれっぽいポエムを生成してコミットしてはいけません。
代わりに、日英版両方に `TODO` という文字列を設定し、ポエムの執筆が必要である旨を報告してください。

### モデルの実装

ほとんどの MOD コンテンツでは、Kotlin で記述され、datagen によって生成されます。

### タグ

コード上から比較的簡単に検索できるほか、JSON で管理される data から判別できます。

### 画像リソース

画像リソースは、最終的には人間が手描きで制作します。
AI エージェントは、画像リソースのある場所に、プレースホルダーとして、ダミーの画像をコミットしてください。
それは、あなたが生成したものか、ライセンスがパブリックドメイン、CC BY、MIT、Apache 2 のいずれかの既存画像を加工したものである必要があります。
プレースホルダーの画像は、プレースホルダーであることが分かるように、不透明部分に `#FF00FF` を加算合成してください。

### アイテム

少なくとも、以下の点について、適切に、過不足なく実装しなければなりません。

- Properties
- 燃料値
- 名称
- ポエム
- モデル
- レシピ - 指定方法が難しいため、既存のものを注意深く見てください。
- タグ
- クリエイティブタブ - バニラのタブではなく、MOD 側の慣例に合わせてください。
- 当該クラス内のオーバーライド - 基本的には、既存のクラスを参考にして作ります。

### ブロック

少なくとも、以下の点について、適切に、過不足なく実装しなければなりません。

- Properties
- 硬度、耐爆性
- 可燃性
- 名称
- blockstate - Kotlin で記述され、datagen によって生成されます。
- モデル
- looting - 指定方法が難しいため、既存のものを注意深く見てください。
- タグ
- 当該クラス内のオーバーライド - 基本的には、既存のクラスを参考にして作ります。
- 新規クラスを実装した場合、BLOCK_TYPE の登録
- そのブロックがアイテムを持つ場合、アイテムとしての実装項目

## 世界観

### 資料

**Lang Table は、最重要の一次資料です。**
**世界観について扱う場合は、必ず、Lang Table を、最初から最後まで、残らず、すべて、熟読しなければなりません。**
少しでも読み飛ばしたり、閲覧自体を省略した場合は、あなたには IFR の世界観について言及する資格は、一切ありません。

---

これは IFRKU 非公式 Wiki のダンプです。
非公式であり、情報源としては二次資料にすぎませんが、非常に手がかりになる情報が大量にまとめられています。
大抵の情報はあるため、疑問に思ったことは、Wiki で調べましょう。

https://github.com/MirrgieRiana/mfku-wiki

---

これは旧公式サイトのミラーです。
公式の一次情報であり、初期の MirageFairy の世界観に関する文書がいくつか置かれています。
世界観について徹底的な調査をしたい場合は、真っ先に読みましょう。

https://github.com/MirrgieRiana/MFKU-Official-Web-Site-Mirror

### 考証

IFR のコンテンツ設計には、世界観考証が不可欠です。
世界観考証とは、コンテンツの世界観的な設定が、現実世界の科学および、作中世界の科学に適合しているかを分析することをいいます。
IFR の世界観では、一部を除いて、現実世界の科学の大半が基本的に有効であり、無効であるものの方が珍しいです。
それに加えて、作品固有の物理法則が同時に適用されています。

世界観考証の際には、ゲームバランスは、一切除いて考えなければなりません。

## コーディング規約

空行にスペースを書くか否かに注意してください。

記述スタイルの一貫性を尊重してください。
リポジトリ内のどこを参照すれば類似の記述が見つかるかを考察し、記述スタイルをあらゆる観点で分析して理解してください。
ルールと一貫性が競合した場合の優先順位は、「義務的ルール＞一貫性＞原則的ルール」です。

### 自然言語

プログラミングにおけるコメントは、「のだ～🌱」口調のかわいい日本語（pyoko-persona スキル準拠）で記述してください。

このリポジトリにおいて、コンテンツの日英版の名称やその他のテキストは、必ずしも対応関係にありません。
英名から日本語名を憶測していい加減に記述するのではなく、必ず Lang Table で確認しなければなりません。
コメント行や更新履歴の内容だからといって、不正確な言葉を用いてもいい訳ではありません。

バニラのコンテンツのテキストも同様に、識別子や英名から憶測するのではなく、必ずバニラの言語ファイルで確認しなければなりません。
バニラの日本語版の言語ファイルは、`unpackedSources` 内ではなく、`unpackedAssets` 内にあります。

コード上に未完成のものをコミットする際は、次の例のように、コメントの冒頭に `TODO` を付けなければなりません。

```kotlin
fun getValue1(): String = "" // TODO ここに本来あるべき状態を記述してください。
fun getValue2(): String = TODO() // TODO コード部分で既に未完成であることが明白な場合も同様です。
```

### Kotlin / kts

最大行長は無限です。行の長さのみを理由にしてコードを折り返してはいけません。

テンプレート文字列の `{ }` は、省略可能な場合は、原則として常に `"$string"` のように省略してください。
ただし、スタイルの一貫性の為であれば敢えて付けるのが正解の場合もあります。

原則として、for 制御構文の代わりに forEach 等の関数を使ってください。
ただし、Channel など、技術的な理由によって制御構文の使用が強制される場合は例外です。

原則として、完全修飾名の代わりに import を使ってください。
import を使うとクラス名が衝突するという理由で、クラス名の方を変更しないでください。
代わりに `import ... as ...` によってエイリアスを使ってください。

入れ子クラスは、入れ子クラス側ではなく、トップレベルのクラス側を import してください。

式形式関数や即代入する変数・プロパティ宣言の型は、必要な理由がある場合を除き、原則として省略してください。
右辺が匿名クラスや、Java 言語で宣言された識別子、過度に具体的な型である場合は省略できません。

関数の本体が複数行に渡る場合は、改行の理由がメソッドチェーンである場合を除き、必ずブロック形式にしてください。

ブロック形式の、関数およびメソッドでは、コンテキスト引数は、前の行に分けて配置します。

一貫性、もしくはその他の合理的な理由がない限り、原則として、デフォルト引数は定義しないでください。

オーバーライドを行うメソッドの引数名は、原則として、親クラスにおける当該メソッドの引数名を使用してください。

1 個の関数からしか呼び出されない関数は、private 関数よりもローカル関数を積極的に使用してください。

KDoc 内での参照は、`[kotlin.Int.unaryPlus]` のように、完全修飾子を使ったリンク構文を使ってください。
これは、参照先が、クラス、メソッド、プロパティ、フィールド、引数であっても同様です。

### リポジトリ固有部分

アイテムのレシピ残留物は、自動的に出力に含められるため、レシピ出力に返却された空容器などを設定してはいけません。

``` `is` ``` メソッドは使わず、`isIn` `isNotIn` 中置関数を使ってください。
Float 型のリテラルは、 `1.0F` のように、必ず末尾に大文字の `F` を付けてください。

原則として、識別子名は、意味論的に基底であるクラス名と対応しなければなりません。
たとえば、`BlockPos` 型の識別子は、`pos` や `position` ではなく、`blockPos` や、これを含む名前にしなければなりません。
また、たとえば、`AreaMiningMultiMineHandler` クラスは、`MultiMineHandler` クラスが、意味論的に基底なクラスです。
ただし、継承したメソッドの場合は、親クラスと同じ識別子名を使わなければなりません。

## generated リソース

generated リソースは、コードの変更とセットです。
generated リソースは手動ではなく `./gradlew datagen` によって生成してください。
コードを変更したら、作業終了前に必ず generated リソースの再生成をしてください。
generated リソースに差分が生じたら、仕上げにそれをコミットしてください。

## 更新履歴

プレイヤーが観測可能な部分への変更が生じた場合は、`CHANGELOG.md` にそれを追記しなければなりません。
当該ファイルはリポジトリルート上にはなく、実際のパスは、`git ls-files | grep CHANGELOG.md` によって検索をしてください。

クリエイティブ専用の機能やコマンドといった、サバイバルプレイヤーが触れることのできない部分も、プレイヤーが観測可能です。
サバイバルで触れることが不可能なコンテンツは、「クリエイティブ専用」カテゴリに記述しなければなりません。

追記位置は「未リリース」セクションであり、既にバージョンが記載されている、リリース済みのセクションに追記をしてはいけません。
「未リリース」セクションが存在しない場合は、バージョンごとのセクションの上に、「未リリース」セクションを追加し、そこに記述してください。

### 接頭辞の選び方

各項目の先頭には、「追加」や「変更」といった接頭辞を付けます。
接頭辞が表すのはプレイヤーから見た更新の種類であり、その項目の文末の動詞とは対応しません。

既存の機能に対する更新であっても、プレイヤーが得られるものが増える場合は、「追加」を選びます。
逆に、プレイヤーが得られるものが減る場合は、「変更」を選びます。

### 接頭辞ごとの整列

セクション内の項目は、接頭辞ごとにまとめて配置し、既存のセクションでの並びを参照して、一貫した順序で並べてください。
既存の項目の接頭辞を書き換える場合は、その項目自体も、書き換え後の接頭辞に対応する位置へ移動しなければなりません。

### 迂回的な記述の禁止

同一バージョンの中で、後続の更新によって先行する更新の文言が無意味になった場合は、その中間の状態を記述してはいけません。
これは、多くの場合、追加もしくは変更の後に、変更もしくは削除を行う、といった組み合わせで生じます。
中間の状態はプレイヤーの目に触れることがないため、必ず、最終的な結果のみを、短絡して記述しなければなりません。

例えば、「醤油コロッケを追加」と「醤油コロッケをソースコロッケに変更」が同一バージョンに現れた場合、
醤油コロッケは結局プレイヤーの目に入ることなく消えたため、短絡して、「ソースコロッケを追加」と記述しなければなりません。

この短絡は、バージョン間をまたいで行ってはいけません。

## コミット

PR が、本質的にその PR の追加コンテンツに限定されない、一般的な領域の差分を持つ場合、先に一般的なものをコミットしなければなりません。

絶対に、依存ライブラリなどの第三者の著作物を独断でコミットしてはいけません
それをすると、そのブランチごと削除しなければならなくなります。

コミットのタイトルおよびメッセージは、Copilot 用の指示文 .github/copilot-instructions.md に準拠してください。
